# gamerslab-poc/docs/_archive — superseded POC docs

Kept for history. The **current** GamersLab POC docs are, in read order:

1. `../../SPEC.md` — UI ↔ Edge ↔ DB contract + the v10 engine, as-built.
2. `../../ARCHITECTURE.md` — the full workflow fleet (all 6 n8n workflows) and their wiring.

| Archived file | Superseded by | Why |
|---|---|---|
| `spec.md` | `../../SPEC.md` | Original aspirational v1 design spec (May 2026) |
| `requirements.md` | `../../SPEC.md` + `../../ARCHITECTURE.md` | v9 build requirements; live engine is v10 |

Still live in `../` (not archived): `cag-block.md` (the CAG brief reference — note the live CAG is
now DB-driven via the Context Builder; this file is the reference copy), `assets/`.
